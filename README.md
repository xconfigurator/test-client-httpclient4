# GitHub 20220411 add
```shell
…or create a new repository on the command line
echo "# test-client-httpclient4" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:xconfigurator/test-client-httpclient4.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin git@github.com:xconfigurator/test-client-httpclient4.git
git branch -M main
git push -u origin main
```